version: '3.8'

services:
  app:
    build: .
    container_name: hdos-app
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=postgresql://Swatiai11414:Swatiai@@@###2003@db:5432/hdos
      - SESSION_SECRET=0d30d9ade1002580c7b3d528963206b9f8292d4c3bc33a63083c738b4c2a54b0
      - SUPER_ADMIN_PASSWORD=Codex@2003
      - NODE_ENV=production
    volumes:
      - uploads:/app/uploads
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:15-alpine
    container_name: hdos-db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=Swatiai11414
      - POSTGRES_PASSWORD=Swatiai@@@###2003
      - POSTGRES_DB=hdos
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U Swatiai11414 -d hdos"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  uploads:
  postgres_data:
